server:
  port: 8083

spring:
  application:
    name: postCreation



  datasource:
    url: jdbc:mysql://localhost:8085/login
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format-sql: true


    database : mysql
    database-platform: org.hibernate.dialect.MySQLDialect

